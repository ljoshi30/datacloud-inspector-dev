# Data 360 Inspector — Development Repo
# This is the FULL internal build + source for testing via VDI.
# The PUBLIC release repo is: ljoshi30/datacloud-inspector (GitHub Pages, stripped build only)

# OS / editor junk
.DS_Store
*.swp
node_modules/

# Local archive of one-off probes / tests
_archive/

# Built zips (regenerate anytime)
*.zip

# Build-state stamp (local only, auto-created by build.js)
chrome-extension/.buildid

# Local-only scratch
.synccheck/
probe-extension/
impl-doc-prototype/
preflight-research/
